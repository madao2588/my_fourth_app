# 演示与联调手册

## 1. 适用场景

这份手册用于本地演示当前访客系统原型，适合自己回顾、给客户演示，或者给同事做快速交接。

## 2. 启动步骤

### 后端

```bash
cd visitor_system/backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

启动成功后访问：

- `http://127.0.0.1:8000/docs`
- `http://127.0.0.1:8000/health`
- `http://127.0.0.1:8000/`

### Web 页面

当前 Web 页面已由后端直接托管，推荐直接访问：

- `http://127.0.0.1:8000/`
- `http://127.0.0.1:8000/visitor.html`
- `http://127.0.0.1:8000/admin.html`

### 微信小程序

用微信开发者工具打开：

- [miniprogram](visitor_system/frontend/miniprogram)

确认小程序请求地址与本地后端联通后即可调试。

## 3. 默认管理员账号

- 总指挥官：`madao / 666666`
- 预置角色账号：`madao1`、`madao2`、`madao3`、`madao4`

注意：

- 当前预置账号默认不强制首次改密
- 如需演示改密锁定流程，可在账号管理中把目标账号设置为 `force_password_change=true`

## 4. 推荐演示流程

### 流程 A：完整闭环

1. 访客在 Web 访客页或小程序提交预约申请
2. 记录返回的 `application_id` 和 `access_code`
3. 管理员登录 Web 管理后台
4. 在待审批列表中批准这条记录
5. 回到访客侧，按手机号查询预约状态
6. 打开电子凭证页，展示审批状态、入场码和二维码
7. 在管理员后台用入场码签到，或者扫码识别二维码
8. 在历史记录、今日概览和系统日志中查看结果

### 流程 B：过期处理

1. 准备一条 `approved` 且未签到的预约
2. 使用管理员后台的“立即清理过期预约”
3. 或执行脚本：

```bash
cd visitor_system/backend
python scripts/expire_appointments.py
```

4. 查看该记录变为 `expired`
5. 在日志页确认过期操作已记录

### 流程 C：管理员安全

1. 用默认管理员登录后台
2. 查看“账户安全”区域中的当前角色与账号状态
3. 需要时主动修改默认密码
4. 在账号管理区新增一个管理员账号
5. 使用账号列表对管理员做启用 / 禁用切换

## 5. 建议重点展示内容

- 访客端申请流程是否简单
- 审批后凭证是否清晰
- 管理员后台是否能完成审批、签到、过期
- 历史记录和日志是否能留痕
- 管理员密码修改是否生效
- 管理员账号是否可管理

## 6. 常用入口

- Swagger：`http://127.0.0.1:8000/docs`
- 健康检查：`http://127.0.0.1:8000/health`
- Web 入口：`http://127.0.0.1:8000/`
- 日志文件：`visitor_system/backend/logs/backend.log`

## 7. 常见问题

### 页面打开了但请求失败

优先检查：

- 后端是否已经启动
- 请求地址是否指向 `127.0.0.1:8000`
- 小程序是否配置了合法请求域名

### 登录失败

优先检查：

- 数据库迁移是否已执行
- 默认管理员是否已初始化
- 输入的用户名密码是否正确

### 登录后后台功能被锁住

优先检查：

- 当前账号是否被设置了 `force_password_change`
- 是否已经完成密码修改

### 小程序看不到二维码

当前二维码展示依赖审批通过状态，只有预约状态为 `approved` 时，凭证页才会显示二维码。
