# 上线前检查清单

这份清单用于把当前原型系统推进到更正式的部署前，帮助逐项确认风险点。

## 1. 环境与配置

- 确认服务器 Python 版本与项目要求一致，当前为 `Python 3.14`
- 确认 `.env` 已配置正式环境变量
- 确认 `JWT_SECRET_KEY` 已替换为强随机值
- 确认默认管理员账号密码已替换
- 确认日志目录和数据目录有写权限

## 2. 数据库

- 确认数据库已执行 `alembic upgrade head`
- 确认是否继续使用 SQLite，还是切换到 PostgreSQL
- 如果切 PostgreSQL，确认连接串、权限和备份策略
- 确认已有测试或演示数据不会污染正式数据

## 3. 后端服务

- 确认 `uvicorn` 或容器启动命令正确
- 确认当前使用的是 SQLite 模式还是 PostgreSQL 模式
- 确认 `/health` 可访问
- 确认 Swagger 文档仅在需要时开放
- 确认自动过期参数合理
- 确认日志级别适合正式环境

## 4. 管理员安全

- 确认管理员账号已更换默认密码
- 确认登录失败日志可追踪
- 确认后台页面不会暴露测试账号信息
- 确认 Bearer Token 存储和传输方式符合预期

## 5. 前端联调

- 确认 Web 页面请求地址指向正式后端
- 确认小程序请求域名已加入合法域名白名单
- 确认二维码展示与扫码流程可用
- 确认历史记录、日志、分页、筛选均正常

## 6. 运维与日志

- 确认日志落盘正常
- 确认日志轮转和磁盘空间策略
- 确认自动过期脚本或后台任务可运行
- 确认异常时能通过日志定位问题

## 7. 演示与验收

- 走一遍“申请 -> 审批 -> 凭证 -> 签到 -> 日志”的完整流程
- 走一遍“申请 -> 审批 -> 过期”的完整流程
- 验证管理员日志中能看到关键事件
- 验证访客侧状态展示与后台状态一致

## 8. 当前阶段建议

如果是内部演示或试点，可以先按这份清单做轻量确认。

如果是正式上线，建议再补：

- PostgreSQL
- HTTPS
- 更正式的账号体系
- 更细粒度权限控制
- 数据备份与恢复方案
